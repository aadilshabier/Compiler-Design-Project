void printf(char* s) {
}

int main() {
	int a[3], r1;
	switch(a[3]) {
	case 'f':
		printf("Lol");
		break;
	default:
		break;
	}

	for (;;) {
		if (r1 == 4)
			continue;
		else if (r1 % 2 == 0) {
			break;
		}
	}
}
